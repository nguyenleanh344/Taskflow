apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: taskflow
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: taskflow-api
        metrics_path: /metrics
        static_configs:
          - targets:
              - taskflow-api:8000
