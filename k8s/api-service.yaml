apiVersion: v1
kind: Service
metadata:
  name: taskflow-api
  namespace: taskflow
spec:
  selector:
    app: taskflow-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30080
  type: NodePort
